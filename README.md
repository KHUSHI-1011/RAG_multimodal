# RAG_multimodal
Multi-Modal RAG system using LangChain and Google Gemini that processes text, tables, and images. It summarizes content, generates embeddings, stores them in a Chroma vector database, and retrieves relevant context to produce accurate, context-aware responses through an end-to-end AI pipeline.
